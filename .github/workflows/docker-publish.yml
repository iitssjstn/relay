name: Build and publish Docker image

# Bouwt bij elke push naar main een nieuwe Docker image en publiceert
# die naar GitHub Container Registry (ghcr.io). Op de VPS hoef je dan
# alleen nog "docker compose pull && docker compose up -d" te draaien
# — geen git pull of lokale build nodig.

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Repo uitchecken
        uses: actions/checkout@v4

      - name: Inloggen bij GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Image-metadata bepalen (tags/labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest
            type=sha,format=short

      - name: Buildx opzetten
        uses: docker/setup-buildx-action@v3

      - name: Build en push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
